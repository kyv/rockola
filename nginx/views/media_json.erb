[
<% @media.each do |media| %>

  { "title":"<%= media.name %>",
    "path":"<%= media.path %>",
    "tipo":"<%= media.type %>",
    "bits":"<%= media.size %>",
    "md5":"<%= media.md5 %>",
    "fecha":"<%= media.created_at %>"
  }

<% end %>
]
